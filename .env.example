# Docker Compose configuration files to load
# By default, runs on CPU. Uncomment the second line to enable GPU execution.
COMPOSE_FILE=docker-compose.yaml
# COMPOSE_FILE=docker-compose.yaml:docker-compose.gpu.yaml
